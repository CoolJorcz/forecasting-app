require 'rails_helper'

RSpec.describe Address, type: :model do
  let(:subject) { create(:address) }

  it 'should not save without a line 1' do
  end

  it 'should not save without a zip' do
  end

  it 'should validate the length of the zip' do
  end

  it 'should not save without '
end
